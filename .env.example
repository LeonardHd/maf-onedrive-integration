# SharePoint / OneDrive configuration
# The SharePoint hostname, e.g. "contoso.sharepoint.com"
SHAREPOINT_HOSTNAME=contoso.sharepoint.com
# Server-relative site path, e.g. "/sites/my-team"
SHAREPOINT_SITE_PATH=/sites/my-team
# (Optional) Folder path relative to the default document library root.
# Leave empty to list files at the root of the library.
ONEDRIVE_FOLDER_PATH=General
# Local directory to download files into
DOWNLOAD_DIR=./downloads
